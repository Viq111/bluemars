language: cpp
compiler:
  - gcc
  - clang
before_install:
  - sudo apt-get update
install:
  - sudo apt-get install cmake
script: 
  - "cmake -DBlueMars_BUILD_UNITTEST=TRUE ." 
  - make
  - ./unittests
os:
  - linux
  - osx
